import { InspectionRecord, Vessel } from '../types';
import { INDONESIAN_PORTS } from '../constants/ports';

// All mock dummy lists are cleared to maintain purely real recorded data from Supabase & user inputs
export const INITIAL_VESSELS: Vessel[] = [];

export const INITIAL_INSPECTIONS: InspectionRecord[] = [];

export { INDONESIAN_PORTS };
